# Copy to .env (or export in your shell). The harness reads keys from the
# environment only - never put a key in a YAML config or commit a real .env.
OPENAI_API_KEY=sk-...
GROQ_API_KEY=gsk_...
TOGETHER_API_KEY=...
LOCAL_API_KEY=dummy
